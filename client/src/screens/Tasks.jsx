import Screen from '../components/Screen.jsx'

export default function Tasks() {
  return (
    <Screen title="Today's Plan" subtitle="Block 4 wires this to /api/tasks/today">
      <div className="panel text-cat-muted">Task dashboard — pending build block 4.</div>
    </Screen>
  )
}
