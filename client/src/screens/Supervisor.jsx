import Screen from '../components/Screen.jsx'

export default function Supervisor() {
  return (
    <Screen title="Command Center" subtitle="Fleet map and alert feed">
      <div className="panel text-cat-muted">Supervisor view - cut to deck mockup unless ahead of schedule.</div>
    </Screen>
  )
}
